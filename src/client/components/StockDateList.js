import React from 'react';
const StockDateList = ({pH}) => {
    // pH is a big object of date and price
    // This print out the big date and stock price
    // console.log(pH)
    // var newDate = []

    // for (var property in pH) {
    //     newDate.push(property)

    // I got the date
    // console.log(newDate);

    return (
      <div>
        {/* The graph or the stock price list should be here    */}
      </div>
    )
}

export default StockDateList;
